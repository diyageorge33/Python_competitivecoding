class Solution:
    def findMaxAverage(self, nums: List[int], k: int) -> float:
        le=len(nums)
        s=sum(nums[:k])
        av=s/k
        m=av

        for i in range(1,le-k+1):
            s=s-nums[i-1]+nums[i+k-1]
            av=s/k
            m=max(m,av)
        return m