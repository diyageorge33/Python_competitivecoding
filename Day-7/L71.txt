class Solution:
    def simplifyPath(self, path: str) -> str:
        st=[]
        a=path.split('/')

        for i in a:
            if i=="" or i==".":
                continue
            elif i=="..":
                if st:
                    st.pop()
            else:
                    st.append(i)
        return "/"+"/".join(st)
